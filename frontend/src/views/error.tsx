import React from "react";

/**
 * The props for the {@link Error} component
 */
export type ErrorProps = {};

/**
 * The component that should render in case an error
 * occurred in the router
 */
export default function Error(props: ErrorProps) {
    return (
        <div></div>
    );
}